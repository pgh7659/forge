# Templates

Reusable project, workflow, prompt, and configuration templates live here.

Templates should document their inputs and expected output files.

- `codex-task-envelope.example.json` defines the initial handoff shape between
  the Hermes assistant control plane and a version-verified Codex executor.
- `codex-result.example.json` defines the evidence returned by that executor.
